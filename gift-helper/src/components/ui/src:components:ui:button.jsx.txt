import React from "react";

export function Button({ children, className, onClick }) {
  return (
    <button className={`px-3 py-1 rounded ${className}`} onClick={onClick}>
      {children}
    </button>
  );
}

export default Button;
